#include <bits/stdc++.h>

using namespace std;

 

uint64_t fibo(uint64_t n){
    uint64_t p, s;
    p = s = 1;
    //for(uint64_t i = 3; i<=
    
}

int main(){
    uint64_t n, m;
    while(cin >> n >> m)
        cout << UINT64_MAX << endl;
        //cout << (fibo(fibo(n)) % m) << endl;
    return 0;
}
