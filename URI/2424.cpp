#include <bits/stdc++.h>

using namespace std;

int main() {
    int a, b;
    cin >> a >> b;
    if (a < 0 || a > 432 || b < 0 || b > 468) cout << "fora" << endl;
    else cout << "dentro" << endl;
    return 0;
}
